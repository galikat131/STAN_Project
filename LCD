#include <LiquidCrystal.h>

// initialize the library with the numbers of the interface pins
LiquidCrystal lcd(12, 11, 5, 4, 3, 2);
// initialize messagelength
int messageLength=13;
int displayMessage=1;
int presses=0;

void setup() {
    pinMode(7, OUTPUT);
    digitalWrite(7, HIGH);
    pinMode(8, INPUT);
  

  // set up the LCD's number of columns and rows: 
  lcd.begin(16, 2);
  // Print a message to the LCD.
  lcd.print("Welcome, ");
  delay(1000);
  lcd.print("hello world!");
}

void loop(){
  lcd.scrollDisplayLeft();
  delay(1000);
}

/*
void loop() {

  int buttonValue = digitalRead(8);
  
  if (buttonValue==1) {
    if (displayMessage !=1 ) {
      lcd.clear();
      lcd.print("It's Pressed!!");
      presses++;
    }

    displayMessage=1;
  }
  
  else if (buttonValue == 0) { 
    if (displayMessage !=0 ) {
    lcd.clear();  
    lcd.print("Press it   ");
    lcd.print(presses);
  }
    displayMessage=0;
  }
  */
//  
//  // scroll 13 positions (string length) to the left 
//  // to move it offscreen left:
//  for (int positionCounter = 0; positionCounter < messageLength; positionCounter++) {
//    // scroll one position left:
//    lcd.scrollDisplayLeft(); 
//    // wait a bit:
//    delay(300);
//  }
//
//  // scroll 29 positions (string length + display length) to the right
//  // to move it offscreen right:
//  for (int positionCounter = 0; positionCounter < messageLength+16; positionCounter++) {
//    // scroll one position right:
//    lcd.scrollDisplayRight(); 
//    // wait a bit:
//    delay(300);
//  }
//  
//    // scroll 16 positions (display length + string length) to the left
//    // to move it back to center:
//  for (int positionCounter = 0; positionCounter < messageLength+16; positionCounter++) {
//    // scroll one position left:
//    lcd.scrollDisplayLeft(); 
//    // wait a bit:
//    delay(300);
//  }
//  
//  // delay at the end of the full loop:
//  delay(1000);

//}
